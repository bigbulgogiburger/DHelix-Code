export interface StatData {
  label: string;
  value: string | number;
  change: number;
  icon: string;
}

export interface User {
  name: string;
  email: string;
  role: string;
  status: string;
}
