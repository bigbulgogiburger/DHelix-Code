/**
 * Phase 5 — Foundational challenge cooldown enforcement (P-1.10 §4).
 *
 * `parseCooldown("\d+[hdw]")` + `checkCooldown(plasmid, action, log)` —
 * pure helpers, no I/O. Override never starts a cooldown for the next amend.
 *
 * Owned by Team 3 — Phase 5 GAL-1 dev-guide §4.
 */

export {};
